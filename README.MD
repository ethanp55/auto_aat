The appendix.pdf file contains the technical appendix. The code.zip file contains our code in one location for easier access. To view the original code when training on data from just the JHG, see the master branch.  To see the original code when training on data from just the chicken game, see the train-chicken-game branch.  Finally, to see the original code when training on data from both the JHG and the chicken game, see the train-jhg-chicken-game branch. 